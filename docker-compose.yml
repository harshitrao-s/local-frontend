services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: erp-frontend-dev

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules

    stdin_open: true
    tty: true

    environment:
      - WATCHPACK_POLLING=true